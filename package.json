{
  "name": "http-files-push",
  "version": "1.0.2",
  "description": "http-files-push makes file uploads a breeze.",
  "main": "index.js",
  "scripts": {
    "deploy": "node ./example/build.js deploy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "fuqqnl",
  "license": "MIT",
  "dependencies": {
    "request": "^2.88.0"
  }
}
